# Local developer dependencies for VS Code notebook editing, QA, and raster checks.
# Colab installs dependencies separately inside each notebook runtime.

earthengine-api
geemap
xarray
rioxarray
rasterio
pandas
numpy
matplotlib
altair
